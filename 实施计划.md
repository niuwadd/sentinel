# ClimeLens (Calyx Home) — 实施计划

> 全屋 3D 可视化 + MQTT 物联网 + LangChain AI 温控系统
> 更新：2026-08-01

---

## 项目现状

| 层级 | 技术栈 | 完成度 |
|------|--------|:---:|
| **前端** | React 19 + Vite + Tailwind 4 + shadcn/ui + zustand + GSAP/motion | ~75% |
| **后端** | NestJS 11 + TypeScript | ~45% |
| **3D** | React Three Fiber + @react-three/drei | ~100% (Phase 2 全部完成) |
| **MQTT** | EMQX 双 Broker (本地+云端) | ~50% (前端双 Broker 切换 + 后端单 Broker 收发完成，后端双订阅/Bridge/离线回传未做) |
| **WebSocket** | Socket.IO 网关 + 前端订阅 | ~100% (Phase 3 完成：传感器/状态/AI 决策三事件推送) |
| **AI** | LangChain.js + LangGraph | 0% |
| **硬件** | ESP32-WROOM-32 + DHT22 + 红外 | 0% |

### 已有代码说明

**前端已完成：**

- UI 框架搭建（Vite + Tailwind 4 + shadcn/ui + 路径别名）
- Design Token 体系（`DESIGN.md` Material You 暖色调，166 行完整色板）
- 4 个页面视图：Home / Rooms / Stats / Settings，含 SVG 趋势图、动画切换
- 5 个 Panel 组件：GlobalStats、RoomStrip（GSAP 滑动指示器）、AiBubble、BrokerStatus、FrostedGlass
- 2 个通用组件：FadeContent（入场动画）、SplitText、TiltedCard、MobileNav
- 3 个 Zustand Store 骨架：sensorStore / deviceStore / aiStore
- 3 个 Hooks 骨架：useMqtt / useSensorData / useAiDecision
- 2 个 Service 骨架：mqtt.service / api.service
- 4 个 TypeScript 类型：sensor / room / ai / tab
- **MQTT 实际连接逻辑完成**（`services/mqtt.service.ts` 双 Broker 连接/订阅/发布，WS 端口 8083）
- **useMqtt 双 Broker 自动切换完成**（本地优先 → 云端降级，连接状态写入 store）
- **ApiService 实际请求逻辑完成**（对接后端 REST API，错误处理）
- **sensorStore 完善**（globalStats 自动计算 avgTemp/avgHumi/onlineCount，brokerHealth/activeBroker）
- **真实数据联动完成**：GlobalStats / RoomStrip / BrokerStatus 已从静态数组改为读取 sensorStore 实时数据
- **3D 户型布局完成**（`house-layout.ts` 按参考图定义 4 个房间尺寸 + 阳台/过道/门厅/厨房/卫生间辅助空间）
- **温度粒子气体可视化完成**（TemperatureBlock 流动粒子场 + 温度平滑变色，替代实体方块）
- **房间点击交互完成**：悬停指针光标、选中高亮（墙体发光 + 地板变色）、右侧详情面板弹窗、点击空白地板取消选择
- **传感器快照初始化完成**（useSensorBootstrap 挂载时从后端 `/api/rooms` 拉取数据写入 sensorStore，3D 场景即时反映后端缓存）
- **WebSocket 服务与 Hook 完成**（`ws.service.ts` + `useWebSocket.ts` 订阅网关三事件，写入 sensorStore / aiStore）
- **Dashboard 主面板完成**（整合 DeviceCard + TempChart + BrokerStatus + AiBubble，替换原房间详情弹窗）
- **DeviceCard 空调控制卡完成**（shadcn Switch/Slider，开关/模式/目标温度/风速/摆风，调用 `POST /api/rooms/:id/ac/control`）
- **TempChart 温度曲线完成**（recharts 加载 24h 历史 + WebSocket 实时增量追加，双轴温度/湿度）
- **Vite 代理配置完成**（`/api` 与 `/socket.io` 转发到 localhost:3000，打通开发环境前后端）

**后端已完成：**

- NestJS 脚手架（app.module / main.ts + 模块注册）
- 配置模块：`config/`（broker / ai / database / env.validation / config.module）
- MQTT 模块：`mqtt/`（mqtt.module + mqtt.service 发布客户端 + mqtt.controller 消费 `house/room/+/data` 与 `house/room/+/status`，并发布 `house/room/+/ac/ctrl`）
- MQTT DTO：`mqtt/dto/`（sensor-message.dto / ac-command.dto，class-validator 校验）
- 房间模块：`room/`（room.module + room.service 房间列表/详情/历史/空调控制 + room.controller `GET /api/rooms`、`GET /api/rooms/:id`、`GET /api/rooms/:id/data`、`POST /api/rooms/:id/ac/control` + dto 校验）
- 共享类型包：`apps/shared/`（sensor.types / mqtt-topics / ac.types + 统一入口 index.ts）
- WebSocket 网关：`gateway/`（sensor.gateway + gateway.module，广播 `sensor:update` / `device:status` / `ai:decision`，MQTT 控制器接入推送，CORS 开放）

**后端待实现：**

- agent / common / health / influx 目录均为空，无业务代码
- 历史数据目前为模拟数据，待 InfluxDB 接入
- 双 Broker 后端订阅、EMQX Bridge、离线回传均未实现

**前端待实现：**

- AI 相关动态化（AiBubble 接 aiStore、useAiDecision 接真实数据）未实现
- 桌面端响应式布局仍需完善

---

## 实施计划

### 前置基础 (Phase 0)

| # | 任务 | 产出文件 | 说明 | 状态 |
|---|------|----------|------|:---:|
| 0.1 | Monorepo 根配置 | 根 `package.json` + `pnpm-workspace.yaml` | 统一管理 apps/server 和 apps/client | √ |
| 0.2 | 共享类型包 `apps/shared/` | 抽取 sensor.types / mqtt-topics / ac.types | 前后端复用 | √ |

---

### Phase 1：打通最小闭环（传感器 → MQTT → 后端 → 前端展示）

#### 1.1 通信基础设施

| # | 任务 | 产出文件 | 说明 | 状态 |
|---|------|----------|------|:---:|
| 1.1 | Docker Compose 本地环境 | `docker-compose.yml` | EMQX (1883/8083/18083) + Redis + InfluxDB | √ |
| 1.2 | 后端环境配置模块 | `server/src/config/` | Broker 地址、LLM API Key、数据库连接配置 + 环境变量校验 | √ |

#### 1.2 NestJS MQTT 微服务

| # | 任务 | 产出文件 | 说明 | 状态 |
|---|------|----------|------|:---:|
| 1.3 | MQTT 模块 | `server/src/mqtt/` | 使用 `@nestjs/microservices` MQTT transport 接入 EMQX | √ |
| 1.4 | 主题订阅 | `mqtt.service.ts` / `mqtt.controller.ts` | 消费 `house/room/+/data`，发布 `house/room/+/ac/ctrl` | √ |
| 1.5 | DTO 定义 | `mqtt/dto/` | SensorDataPayload / AcCommandPayload 类型校验 | √ |

#### 1.3 NestJS 房间模块

| # | 任务 | 产出文件 | 说明 | 状态 |
|---|------|----------|------|:---:|
| 1.6 | Room CRUD | `server/src/room/` | 房间列表查询、单个房间传感器最新数据、历史数据 | √ |
| 1.7 | 空调控制 API | `room.controller.ts` | `POST /api/rooms/:id/ac/control` → 通过 MQTT 下发红外指令 | √ |

#### 1.4 前端 MQTT & API 对接

| # | 任务 | 产出文件 | 说明 | 状态 |
|---|------|----------|------|:---:|
| 1.8 | MqttService 实现 | `services/mqtt.service.ts` | mqtt.js 连接/断开/订阅/发布，WS 端口 8083 | √ |
| 1.9 | useMqtt Hook | `hooks/useMqtt.ts` | 双 Broker 自动切换（本地优先→云端降级）、连接状态管理 | √ |
| 1.10 | ApiService 实现 | `services/api.service.ts` | 对接后端 REST API，错误处理 | √ |
| 1.11 | useSensorData Hook | `hooks/useSensorData.ts` | 从 sensorStore 读取指定房间实时数据 | √ |
| 1.12 | sensorStore 完善 | `store/sensorStore.ts` | 添加 globalStats 自动计算（avgTemp/avgHumi/onlineCount） | √ |

#### 1.5 前端 UI 数据联动

| # | 任务 | 产出文件 | 说明 | 状态 |
|---|------|----------|------|:---:|
| 1.13 | GlobalStats 接真实数据 | `components/Panel/GlobalStats.tsx` | 替换 rooms 静态数组 → sensorStore 实时数据 | √ |
| 1.14 | RoomStrip 接真实数据 | `components/Panel/RoomStrip.tsx` | 房间卡片温度/湿度从 sensorStore 读取 | √ |
| 1.15 | BrokerStatus 动态化 | `components/Panel/BrokerStatus.tsx` | 根据 brokerHealth 状态显示绿/黄/红指示灯 | √ |

---

### Phase 2：3D 可视化

| # | 任务 | 产出文件 | 说明 | 状态 |
|---|------|----------|------|:---:|
| 2.1 | 安装依赖 | `package.json` | `@react-three/fiber` `@react-three/drei` `three` `@types/three` | ✅ |
| 2.2 | 3D Canvas 场景入口 | `components/Scene/House.tsx` | R3F Canvas + 相机控制 (OrbitControls) + 光照系统 | ✅ |
| 2.3 | 房间墙体模型 | `components/Scene/Room.tsx` | BoxGeometry 构建简易墙体/地板，支持 GLB 模型加载 | ✅ |
| 2.4 | 温度粒子气体 | `components/Scene/TemperatureBlock.tsx` | 流动粒子场根据温度动态着色（蓝→绿→黄→红），平滑过渡 | ✅ |
| 2.5 | 温度标签 | `components/Scene/Room.tsx` | drei Text/HTML 标注房间名称和实时温度 | ✅ |
| 2.6 | 空调设备模型 | `components/Scene/AcUnit.tsx` | 简易几何体组合（或加载 GLB），开关态/风速风扇动画 | ✅ |
| 2.7 | 房间点击交互 | `components/Scene/House.tsx` | Raycaster 检测 → 选中房间 → 高亮边框 + 弹出 Dashboard（悬停光标、空白取消选择） | ✅ |
| 2.8 | 数据驱动更新 | `components/Scene/House.tsx` | 订阅 sensorStore → 粒子颜色/标签数值实时刷新，后端快照初始化 | ✅ |

---

### Phase 3：控制面板 & WebSocket 网关

| # | 任务 | 产出文件 | 说明 | 状态 |
|---|------|----------|------|:---:|
| 3.1 | NestJS WebSocket 网关 | `server/src/gateway/` | `@nestjs/websockets` 推送 `sensor:update` / `device:status` / `ai:decision` 三事件 | ✅ |
| 3.2 | Dashboard 主面板 | `components/Panel/Dashboard.tsx` | 整合 DeviceCard + TempChart + BrokerStatus + AiBubble | ✅ |
| 3.3 | DeviceCard 实现 | `components/Panel/DeviceCard.tsx` | 空调控制卡片：开关/模式/温度滑块/风速/摆风（shadcn Switch/Slider） | ✅ |
| 3.4 | TempChart 实现 | `components/Panel/TempChart.tsx` | recharts 实时温度曲线，WebSocket 驱动增量更新 | ✅ |
| 3.5 | 前端 WebSocket 连接 | `hooks/useWebSocket.ts` | 订阅 WS 推送流，更新 store | ✅ |

---

### Phase 4：AI 温控引擎

| # | 任务 | 产出文件 | 说明 | 状态 |
|---|------|----------|------|:---:|
| 4.1 | LangChain 环境搭建 | `server/package.json` | `@langchain/core` `@langchain/openai` `langgraph` |  |
| 4.2 | Agent 模块骨架 | `server/src/agent/` | module / service / tools 目录结构 |  |
| 4.3 | Tool: check_env | `agent/tools/env.tool.ts` | 查询指定房间当前温湿度，从 Redis/MQTT 获取 |  |
| 4.4 | Tool: control_ac | `agent/tools/ac.tool.ts` | 下发空调控制指令（开关/模式/温度/风速） |  |
| 4.5 | LangGraph 工作流 | `agent/graph/thermostat.graph.ts` | 感知→分析→决策→执行→反思 五步循环 |  |
| 4.6 | Agent 触发策略 | `agent/agent.service.ts` | 定时（每 5 分钟）/ 事件驱动（温度异常）触发分析 |  |
| 4.7 | AI 决策记录 | `agent/agent.service.ts` | 存储决策日志 (PostgreSQL)，供前端 AiBubble 展示 |  |
| 4.8 | 断网降级 | `agent/agent.service.ts` | LLM 不可用时自动切换本地阈值逻辑 |  |

#### 前端 AI 对接

| # | 任务 | 产出文件 | 说明 | 状态 |
|---|------|----------|------|:---:|
| 4.9 | AiBubble 动态化 | `components/Panel/AiBubble.tsx` | 从 aiStore 读取最新决策，显示推理内容+建议 |  |
| 4.10 | useAiDecision Hook | `hooks/useAiDecision.ts` | 从 aiStore 读取决策数组+活跃状态 |  |

---

### Phase 5：数据持久化

| # | 任务 | 产出文件 | 说明 | 状态 |
|---|------|----------|------|:---:|
| 5.1 | InfluxDB 适配模块 | `server/src/influx/` | 传感器时序数据写入 + 聚合查询（小时/天/周） |  |
| 5.2 | PostgreSQL 实体 | TypeORM/Prisma Schema | User / Room / Device / DecisionLog |  |
| 5.3 | 历史数据 API | `room.controller.ts` | `GET /api/rooms/:id/data?range=24h\|7d\|30d` |  |

---

### Phase 6：双 Broker 高可用

| # | 任务 | 产出文件 | 说明 | 状态 |
|---|------|----------|------|:---:|
| 6.1 | 前端双 Broker 切换 | `hooks/useMqtt.ts` | 本地优先，断线自动切云端，恢复后切回本地 | √ |
| 6.2 | 后端双 Broker 订阅 | `mqtt/mqtt.service.ts` | 同时订阅本地+云端，消息去重 |  |
| 6.3 | EMQX Bridge | `emqx.conf` | 本地↔云端双向主题同步配置 |  |
| 6.4 | 离线数据回传 | `mqtt/mqtt.service.ts` | 本地 Broker 缓存断网期间数据，恢复后批量同步 |  |

---

### Phase 7：硬件固件

| # | 任务 | 产出文件 | 说明 | 状态 |
|---|------|----------|------|:---:|
| 7.1 | PlatformIO 项目初始化 | `firmware/smart-thermostat/` | platformio.ini 配置 ESP32 开发板 |  |
| 7.2 | DHT22 传感器驱动 | `firmware/smart-thermostat/src/` | 每 30s 采集温湿度 |  |
| 7.3 | MQTT 双 Broker 连接 | `mqtt.h` | 本地 TCP 1883 + 云端 TLS 8883 |  |
| 7.4 | 红外学习 | `ir.h` | VS1838 接收空调遥控码，存储到 NVS |  |
| 7.5 | 红外发射 | `ir.h` | 38KHz PWM 模拟遥控信号，支持多种码型 |  |
| 7.6 | 本地阈值容灾 | `main.cpp` | WiFi 断连→本地逻辑（<22°C 停制冷 / >30°C 强制制冷） |  |
| 7.7 | OTA 固件升级 | `main.cpp` | 通过 MQTT 主题接收固件更新 |  |

---

### Phase 8：部署 & 测试

| # | 任务 | 说明 | 状态 |
|---|------|------|:---:|
| 8.1 | 云端 Docker Compose | NestJS + LangChain Agent + PostgreSQL + InfluxDB |  |
| 8.2 | Kubernetes 编排 | `deploy/k8s/` 部署文件 |  |
| 8.3 | 前端生产构建 | Vite build + Nginx 静态托管 + 反向代理 |  |
| 8.4 | 后端单元测试 | Jest 覆盖率 > 70% |  |
| 8.5 | 后端 e2e 测试 | MQTT 模拟设备 → API → Agent 全链路 |  |
| 8.6 | 前端组件测试 | Vitest + React Testing Library |  |
| 8.7 | 集成测试 | ESP32 硬件 + 本地 EMQX + NestJS + React 完整场景 |  |

---

## 执行优先级路线图

```
Week 1  ████████████  Phase 1 (最小闭环)              ✅ 已完成
         Docker EMQX → NestJS MQTT → 前端数据联通

Week 2  ████████████  Phase 2 (3D 可视化)              ✅ 已完成
         R3F Canvas → 户型布局 → 粒子温度场 → 点击交互 → 数据驱动

Week 3  ████████████  Phase 3 (控制面板)              ✅ 已完成
         WebSocket 网关 → Dashboard → DeviceCard → TempChart

Week 4  ░░░░░░░░░░░░  Phase 4 (AI 引擎)               待开始
         LangChain Agent → Tool → LangGraph → AiBubble 动态

Week 5  ░░░░░░░░░░░░  Phase 5 + 6 (持久化 + 高可用)    待开始
         InfluxDB → PostgreSQL → 双 Broker 切换（前端已完成）

Week 6  ░░░░░░░░░░░░  Phase 7 (硬件固件)              待开始
         ESP32 固件开发 + 烧录测试

Week 7  ░░░░░░░░░░░░  Phase 8 (部署 + 测试)           待开始
         Docker/K8s 部署 → 全链路测试 → 性能优化
```

---

## 统计

| 类别 | 已完成 | 待实现 | 合计 |
|------|:---:|:---:|:---:|
| 前置基础 | 2 | 0 | 2 |
| 通信 & MQTT | 5 | 0 | 5 |
| 后端业务 | 2 | 0 | 2 |
| 前端对接 | 5 | 0 | 5 |
| 前端 UI 联动 | 3 | 0 | 3 |
| 3D 可视化 | 8 | 0 | 8 |
| 控制面板 & WS | 5 | 0 | 5 |
| AI Agent | 0 | 10 | 10 |
| 数据持久化 | 0 | 3 | 3 |
| 双 Broker 高可用 | 1 | 3 | 4 |
| 硬件固件 | 0 | 7 | 7 |
| 部署测试 | 0 | 7 | 7 |
| **总计** | **31** | **30** | **61** |
